#!/usr/bin/env ruby


require 'sinatra'

set :bind, 'pinto.webdev.x'
set :port, 9000

get %r{/build/(.*)} do |project|
    command = "cd /home/pinto/jams &&  ~/jam/bin/pjam -p #{project}  2>&1"
    content_type :text
    `#{command}`
end

